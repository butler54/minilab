apiVersion: v2
name: openshell
description: OpenShell agent sandbox platform — wrapper around the upstream NVIDIA chart with minilab-pattern defaults (feature 006-openshell-gitops-install)
type: application
version: 0.1.0
dependencies:
  # Vendored at sync time into charts/: `helm dependency build`.
  # Upstream source: oci://ghcr.io/nvidia/openshell/helm-chart (research.md D1).
  - name: helm-chart
    alias: openshell
    version: 0.0.116
    repository: oci://ghcr.io/nvidia/openshell
